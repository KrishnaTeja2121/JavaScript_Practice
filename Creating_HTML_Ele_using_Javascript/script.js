//! 1. Select the Target Element
const appContainer=document.getElementById("app");
const generateBtn=document.getElementById("generateBtn");

//! 2. Create a new HTML elements (document.createElement())
function createProfileCard(){
    //creating the profile card container
    const profileCard=document.createElement("div");
    profileCard.className="profile-card";
}
//! 3. Add content to elements (innerHTML/textContent)
//! 4. Add classes or id's if needed // add any attributes
//! 5. Append the elements to build the structure
//! Append to the DOM






